import { CanvasStore, DestroyedService } from '@angular-three/core';
import { EventEmitter, NgZone, OnInit } from '@angular/core';
import { AudioListener } from 'three';
import * as i0 from "@angular/core";
export declare class AudioListenerDirective implements OnInit {
    private readonly canvasStore;
    private readonly destroyed;
    private readonly ngZone;
    filter?: AudioNode;
    timeDelta?: number;
    ready: EventEmitter<AudioListener>;
    private _listener;
    constructor(canvasStore: CanvasStore, destroyed: DestroyedService, ngZone: NgZone);
    ngOnInit(): void;
    get audioListener(): AudioListener;
    static ɵfac: i0.ɵɵFactoryDeclaration<AudioListenerDirective, [{ skipSelf: true; }, null, null]>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<AudioListenerDirective, "ngt-audio-listener", ["ngtAudioListener"], { "filter": "filter"; "timeDelta": "timeDelta"; }, { "ready": "ready"; }, never>;
}
