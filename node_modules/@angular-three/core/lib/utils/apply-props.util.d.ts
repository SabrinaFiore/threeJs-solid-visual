import type { ThreeInstance, UnknownRecord } from '../typings';
/**
 * Apply props on instances
 *
 * @internal
 * @private
 */
export declare function applyProps(instance: ThreeInstance, props?: UnknownRecord): void;
