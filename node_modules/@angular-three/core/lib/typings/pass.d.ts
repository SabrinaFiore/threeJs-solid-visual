export declare type WithoutCameraConstructorParameters<T, TReturn = T extends [infer First, ...infer Rest] ? Rest : T> = TReturn;
export declare type WithoutSceneCameraConstructorParameters<T, TReturn = T extends [infer First, infer Second, ...infer Rest] ? Rest : T> = TReturn;
